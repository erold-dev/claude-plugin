{
  "description": "Erold workflow hooks - context loading, file tracking, session management",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh",
            "timeout": 15
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/log-file-change.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If there's an active Erold task from this session: (1) If work complete, call complete_task with summary. (2) If incomplete, call add_task_comment with progress summary for next session. Then respond {\"ok\": true}.",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
